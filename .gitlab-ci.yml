before_script:
 - apt-get update -qq
 - apt-get install libsdl2-dev libsdl2-image-dev libsd2-ttf-dev -y

tests:
    script: tests/run.sh
    stage: test

build_test:
    script:
        - tests/build.sh
    stage: build